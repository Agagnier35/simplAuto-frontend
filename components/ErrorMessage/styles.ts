import styled from 'styled-components';

const StyledError = styled.div`
  .card-body {
    color: red;
    display: flex;
    align-items: center;

    svg {
      margin-right: 1rem;
    }

    p {
      margin: 0;
    }
  }
`;

export default StyledError;
