export default interface Translations {
  login: {
    title: string;
  };
  general: {
    email: string;
    password: string;
    changeLangage: string;
  };
  errors: {
    invalidEmail: string;
    invalidPassword: string;
    authError: string;
  };
}
