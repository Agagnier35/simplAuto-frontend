overwrite: true
# schema: 'https://simplauto-backend.herokuapp.com/' # remote
schema: 'http://localhost:4000' # local
documents: '**/*.graphql'
generates:
  generated/graphql.ts:
    plugins:
      - 'typescript-common'
      - 'typescript-client'
      - 'typescript-server'
