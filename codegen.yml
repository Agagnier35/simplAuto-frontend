overwrite: true
schema: 'https://simplauto-backend.herokuapp.com/' # switch to this if you need to test with your current backend: http://localhost:4000
documents: '**/*.graphql'
generates:
  generated/graphql.ts:
    plugins:
      - 'typescript-common'
      - 'typescript-client'
      - 'typescript-server'
