import styled from 'styled-components';

const SuppressBtnStyle = styled.form`
  img {
    max-width: 100%;
    float: left;
  }
  button {
    max-width: 3%;
  }
`;

export default SuppressBtnStyle;
