import styled from 'styled-components';

const PagingStyle = styled.form`
  .mainComponent {
    text-align: center;
  }

  .centerButtonToolBar {
    margin: auto;
  }
`;

export default PagingStyle;
