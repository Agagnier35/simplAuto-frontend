export default interface Translations {
  login: {
    title: string;
    invalidPassword: string;
    invalidEmail: string;
  };
  general: {
    email: string;
    password: string;
    changeLangage: string;
  };
}
